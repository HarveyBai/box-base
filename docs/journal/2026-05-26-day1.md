
# Day 1 复盘 — 2026-05-26

## 今天完成的事
- 初始化 backend/ Python 项目骨架
- 实现 /health 端点（FastAPI + Pydantic v2 响应模型）
- 建立 TDD 流程：先写测试，后写实现
- 配置 ruff / mypy / pytest

## 遇到的 3 个意外
1. uv init 生成的占位 main.py 在根目录，不是包目录——需要手动清理
2. AI 在 TDD 流程中跳过了 Step D/E，没有主动 STOP 等批准
3. Windows 终端的 CLIXML 输出让 pytest 日志出现乱码（重定向到文件后可读）

## AI 超预期的 1 个点
- test_health.py 的质量极高：ASGITransport 写法、4 个有效断言、f-string 错误提示、中英文注释分工——完全符合 AGENTS.md 规范

## AI 需要改进的 1 个点
- TDD 流程中没有严格遵守 STOP 节奏，需要在提示词里加更明确的"STOP and WAIT"指令

## 明天（Day 2）的工作
- 配置 pre-commit hooks（ruff + mypy 提交前自动检查）
- 搭建 GitHub Actions CI 流水线
- 验证"推错误代码 → CI 红灯拦截"

## 对 AI 工作方式的调整
- 每个 Step 末尾加 "STOP — 等待你说'继续 Step X'"，已验证有效
- 下次提示词里加：如果 AI 连续执行多步，我会打断并要求回到上一步
